let cart = [];

function addToCart(productId) {
    // Add product to cart array and update cart display
}

function toggleCart() {
    const cartElement = document.getElementById('shopping-cart');
    cartElement.style.transform = cartElement.style.transform === 'translateX(100%)' ? 'translateX(0)' : 'translateX(100%)';
}
