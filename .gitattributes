# plaintext     (this file is written in plaintext syntax)

# 自动检测文本文件并执行LF行尾标准化
* text=auto eol=lf

# 为已知的文件类型设置自定义属性
*.py text eol=lf  # Python 文件使用 LF 行尾
*.cpp text eol=lf # C++ 源文件使用 LF 行尾
*.h text eol=lf   # C/C++ 头文件使用 LF 行尾
*.java text eol=lf # Java 文件使用 LF 行尾
*.html text eol=lf # HTML 文件使用 LF 行尾
*.css text eol=lf  # CSS 文件使用 LF 行尾
*.js text eol=lf   # JavaScript 文件使用 LF 行尾
*.json text eol=lf # JSON 文件使用 LF 行尾
*.xml text eol=lf  # XML 文件使用 LF 行尾
*.yml text eol=lf  # YML 文件使用 LF 行尾
*.yaml text eol=lf # YAML 文件使用 LF 行尾
*.md text eol=lf   # Markdown 文件使用 LF 行尾

# JetBrains IDE 特定文件
.idea/ binary # 将 .idea 目录标记为二进制，避免 Git 处理这些文件

# Visual Studio Code 特定文件
.vscode/ binary # 将 .vscode 目录标记为二进制，避免 Git 处理这些文件

# 二进制文件
*.png binary  # PNG 图片为二进制文件
*.jpg binary  # JPG 图片为二进制文件
*.jpeg binary # JPEG 图片为二进制文件
*.gif binary  # GIF 图片为二进制文件
*.ico binary  # ICO 图标为二进制文件
*.svg binary  # SVG 图片为二进制文件
*.pdf binary  # PDF 文档为二进制文件

# 压缩文件
*.zip binary    # ZIP 压缩文件为二进制文件
*.tar.gz binary # TAR.GZ 压缩文件为二进制文件
*.rar binary    # RAR 压缩文件为二进制文件

# 可执行文件
*.exe binary  # Windows 可执行文件为二进制文件
*.dll binary  # Windows 动态链接库为二进制文件
*.so binary   # Linux 共享对象为二进制文件
*.dylib binary # macOS 动态库为二进制文件
*.bin binary  # 通用二进制文件

# 忽略由 JetBrains IDEs 生成的文件
.idea/ # 忽略 .idea 目录

# 忽略由 Visual Studio Code 生成的文件
.vscode/ # 忽略 .vscode 目录

# 忽略典型的临时文件
*.log  # 忽略日志文件
*.tmp  # 忽略临时文件
*.temp # 忽略临时文件